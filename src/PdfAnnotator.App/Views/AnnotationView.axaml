<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="PdfAnnotator.App.Views.AnnotationView">
    <UserControl.DataContext>
        <Binding Path="Annotation" RelativeSource="{RelativeSource AncestorType=Window}"/>
    </UserControl.DataContext>
    <Grid ColumnDefinitions="320,*" RowDefinitions="Auto,*">
        <StackPanel Margin="12" Spacing="8">
            <TextBlock Text="Режим: аннотирование" FontWeight="Bold"/>
            <TextBox Width="280" Watermark="Путь к PDF" Text="{Binding PdfPath, UpdateSourceTrigger=PropertyChanged}"/>
            <StackPanel Orientation="Horizontal" Spacing="8">
                <Button Content="Открыть PDF" Command="{Binding LoadPdfCommand}"/>
                <TextBlock Text="{Binding PageCount, StringFormat='Страниц: {0}'}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Spacing="8">
                <TextBlock Text="Страница"/>
                <NumericUpDown Width="80" Minimum="1" Maximum="{Binding PageCount}" Value="{Binding CurrentPage}"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Spacing="8">
                <TextBlock Text="Пресет"/>
                <ComboBox Width="160" Items="{Binding Presets}" SelectedItem="{Binding SelectedPreset}" DisplayMemberPath="Name"/>
                <Button Content="Обновить" Command="{Binding ReloadPresetsCommand}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Spacing="8">
                <Button Content="Сохранить пресет" Command="{Binding SavePresetCommand}"/>
                <Button Content="Сохранить итоговый PDF" Command="{Binding SaveAnnotatedPdfCommand}"/>
            </StackPanel>

            <StackPanel Spacing="4">
                <TextBlock Text="Параметры текста"/>
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <TextBlock Text="Font size"/>
                    <NumericUpDown Width="80" Minimum="6" Maximum="72" Value="{Binding FontSize}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <TextBlock Text="Color"/>
                    <TextBox Width="100" Text="{Binding ColorHex}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <TextBlock Text="Angle"/>
                    <NumericUpDown Width="80" Minimum="0" Maximum="360" Value="{Binding Angle}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <TextBlock Text="Font"/>
                    <ComboBox Width="160" Items="{Binding Fonts}" SelectedItem="{Binding FontName}"/>
                </StackPanel>
            </StackPanel>
        </StackPanel>

        <ScrollViewer Grid.Column="1" Margin="12" Background="Gray">
            <Grid>
                <Image x:Name="AnnotImage"
                       Source="{Binding PageBitmap}"
                       Stretch="Uniform"
                       PointerPressed="OnPointerPressed"/>
                <Canvas IsHitTestVisible="False">
                    <TextBlock Text="{Binding SelectedCodePreview}"
                               Canvas.Left="{Binding PreviewX}"
                               Canvas.Top="{Binding PreviewY}"
                               FontSize="{Binding FontSize}"
                               RenderTransformOrigin="0,0">
                        <TextBlock.RenderTransform>
                            <RotateTransform Angle="{Binding Angle}"/>
                        </TextBlock.RenderTransform>
                    </TextBlock>
                </Canvas>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
